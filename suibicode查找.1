include <stdio.h>
int main()
{
    int a[100];
    int i,j,p=0;
    for(i=0;i<100;i++)
    scanf("%d",&a[i]);
    for(i=0;i<100;i++)
    {
        if(a[i]=5)
        {
            p=1;
            break;
        }
        else;
    }
    if(p=1)
    printf("YES");
    else
    printf("NO");
    return 0;
    }
