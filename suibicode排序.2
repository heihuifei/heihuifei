include<stdio.h>
int main()
{
    int a[100],i,j,temp=0;
    scanf("%d",&n);
    ....
    for(i=0;i<n;i++)
    {
        for(j=1;j<n-i;j++)
        if(a[i]>a[i+j])
        {
            temp=a[i];
            a[i]=a[i+j];
            a[i+j]=temp;
        }
        else;
    }
    for(i=0;i<n;i++)
    printf("%d",a[i]);
    return 0;
}
